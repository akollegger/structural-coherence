# Data

Document the mined repositories, migration-identification criteria, and licensing here
once RFC-007 is frozen. Keep raw data out of git if it exceeds a few MB — `.gitignore` it
and note the external location instead.
