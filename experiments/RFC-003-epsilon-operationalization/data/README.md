# Data

Document corpus sources, construction/sampling method, and licensing here once
RFC-003 is frozen. Keep raw data out of git if it exceeds a few MB — `.gitignore`
it and note the external location (bucket, DVC remote, etc.) in this file instead.
