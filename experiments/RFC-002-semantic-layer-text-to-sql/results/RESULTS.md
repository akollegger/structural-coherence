# Results — RFC-002

**Status:** Not yet run.

## Verdict

_(confirmed / refuted / mixed, per prediction below — fill in once the experiment runs,
before any discussion)_

| Prediction | Verdict |
|---|---|
| P1 — dose-response (raw accuracy declines monotonically with denormalization) | _pending_ |
| P2 — benefit scales with lost coherence (lift increases monotonically with denormalization) | _pending_ |
| P3 — naming-ladder rungs each add accuracy and compound | _pending_ |
| P4 — enrichment rungs scale with denormalization; interpretation rung does not | _pending_ |
| P5 — volume dose-response (superlinear decline with schema volume) | _pending_ |
| P6 — the composite (volume × denormalization) predicts better than either axis alone | _pending_ |

## RFC-002 Predictions (for reference)

See `proposals/RFC-002-semantic-layer-text-to-sql.md` §3 for full statements and falsification
conditions. P3 and P4 are the load-bearing predictions that distinguish "restored structure"
from "better labels"; P6 is the prediction most specific to RFC-001, since it tests the
two-exponent bound's interaction rather than either factor alone.

## Findings

_(fill in after running — structure discussion around Section 5 of the RFC, "What each
outcome would mean for RFC-001")_

## Figures

See `figures/`.
