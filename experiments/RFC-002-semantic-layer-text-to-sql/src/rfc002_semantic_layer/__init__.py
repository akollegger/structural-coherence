"""RFC-002: semantic-layer text-to-SQL experiment.

Skeleton package only — no pipeline logic yet. RFC-002 is still a drafting
pre-registration (v3); per this repo's AGENTS.md, implementation work should
not begin until the RFC is frozen. This package exists to hold the tooling
configuration (pyproject.toml, ruff, pytest) established ahead of that
freeze.
"""

__version__ = "0.1.0"
