# Data

Document the corpus, embedding model, and growth schedule here once RFC-006 is frozen.
Keep raw data out of git if it exceeds a few MB — `.gitignore` it and note the external
location instead.
