# Data

Document the natural corpora and their clustering-strength annotations here once RFC-007
is frozen. Keep raw data out of git if it exceeds a few MB — `.gitignore` it and note the
external location instead.
