# Data

Document the task set, the four memory-architecture implementations (unstructured log,
embedding RAG, schema-structured, graph community/motif), and the horizon schedule here
once RFC-008 is frozen. Keep raw data/transcripts out of git if they exceed a few MB —
`.gitignore` them and note the external location instead.
