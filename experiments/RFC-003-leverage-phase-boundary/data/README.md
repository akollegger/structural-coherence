# Data

Document the matched corpora and their growth policies (fixed / sublinear / linear /
matched-to-$\beta$ structural growth) here once RFC-003 is frozen. Keep raw data out of
git if it exceeds a few MB — `.gitignore` it and note the external location instead.
